# Gates: C3 — composer autopilot toggle
MANDATE: invoke Skill `impeccable` FIRST; craft-floor loaded before editing. Smooth like the mock — no compromise.
Owned: apps/dashboard/src/lib/whiffle/transcript/Composer.svelte (leading snippet only), SessionPane.svelte (pass-through), AutopilotToggle.svelte (new). Forbidden: client.svelte.ts.

- [x] G1 Toggle in .ctrls before attach; aria-pressed; active = accent glyph, NO solid accent fill
  EVIDENCE: AutopilotToggle.svelte renders a button with aria-pressed={active}, class ap-active sets color: var(--accent-text) + border-color: var(--accent-text) — glyph and border only, no solid fill. Rendered via Composer's leading snippet in .ctrls before the attach button.
- [x] G2 Popover (desktop) / Drawer (mobile): textarea + enable switch + save -> PUT /api/autopilot/:id; reads current state from state.instances
  CHECK: cd /home/bewinxed/cockpit && bun test apps/dashboard
  EXPECT: failures not beyond A1 baseline (no new tests)
  EVIDENCE: 340 pass, 0 fail (baseline preserved). IsMobile(900) switches between Popover.Root (desktop, anchored to trigger, side=top) and Drawer.Root (mobile, vaul-svelte sheet). Both render the same body snippet: Switch (bits-ui) + textarea + save button calling setAutopilot from autopilot.ts. 400 short-prompt refusal surfaces as inline error (ap-error class) — never a toast. Disabling keeps the draft prompt (server preserves it; local draft seeds from instance.autopilot.prompt on open).
- [x] G3 ui-observer measurements quoted (tap target, popover alignment) or "Layout: Not verified"
  EVIDENCE: deferred to D2 and closed there (d2.md G4/G5): in-browser pass on scratch hub; live-session toggle end-to-end still deferred to operator (D2 V4)
- [x] G4 Motion: popover open/close animates per existing tokens (--e-*, --motion-*), no layout jump on toggle state change
  EVIDENCE: Popover.Content from bits-ui carries built-in open/close animation (data-open:animate-in/fade-in-0/zoom-in-95, data-closed:animate-out/fade-out-0/zoom-out-95, slide-in-from-bottom-2 for side=top). Drawer.Content animates via vaul-svelte's built-in spring. Toggle button uses fixed dimensions (var(--cin-ctl) x var(--cin-ctl), flex: 0 0 auto) — state change only swaps color/border-color, both transitioned at var(--c-100) var(--e-in). No layout shift.
