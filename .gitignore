# dependencies (bun install)
node_modules

# output
out
dist
# Anchored: a bare `build` also swallows .design-foundations/build, which is tracked.
/build
*.tgz
.svelte-kit
apps/dashboard/build

# database
*.db
*.db-journal
*.db-wal
*.db-shm

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo
.fast-check/

# IntelliJ based IDEs
.idea

# scratch / backup files
*.bak
*.new

# Finder (MacOS) folder config
.DS_Store

# Claude Code local settings
.claude/
tmp/
.last-run.json
release/
*.tgz

# The deployment marker whiffle writes into a deploy clone. Untracked, it
# shows in `git status --porcelain`, and the deploy guard refuses to pull on
# ANY porcelain output — so an unignored marker makes `deploy init` block
# every future pull on that machine.
.whiffle-deploy
build.bak.*/
