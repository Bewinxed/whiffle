diff --git a/dist/svelte/components/Panel.svelte b/dist/svelte/components/Panel.svelte
index dbff140c5ba1e9aa406982509ea276e1d785737d..0574f0ef9b39d9d49d0f969b910cebd31e5e4733 100644
--- a/dist/svelte/components/Panel.svelte
+++ b/dist/svelte/components/Panel.svelte
@@ -23,7 +23,7 @@
   // The store owns open/collapsed state so it can be driven programmatically.
   let storeOpen = $state<boolean | undefined>(DialStore.getPanelOpen(panel.id));
   const isOpen = $derived(storeOpen ?? defaultOpen);
-  let values = $state<Record<string, DialValue>>(DialStore.getValues(panel.id));
+  let values = $state.raw<Record<string, DialValue>>(DialStore.getValues(panel.id));
   let presets = $state<Preset[]>(DialStore.getPresets(panel.id));
   let activePresetId = $state<string | null>(DialStore.getActivePresetId(panel.id));
 
diff --git a/dist/svelte/components/Timeline/TimelineSection.svelte b/dist/svelte/components/Timeline/TimelineSection.svelte
index 100b7c828d8aca7b09f55b58fcba259602c77ada..8d13dedca9adf09e20fb98e194395e1f24ec2b6b 100644
--- a/dist/svelte/components/Timeline/TimelineSection.svelte
+++ b/dist/svelte/components/Timeline/TimelineSection.svelte
@@ -104,8 +104,8 @@
   let expandedTracks = $state(new Set<string>());
   let zoom = $state(1);
   let viewStart = $state(0);
-  let values = $state<Record<string, DialValue>>(DialStore.getValues(untrack(() => meta.id)));
-  let transport = $state<TimelineTransport>(TimelineStore.getTransport(untrack(() => meta.id)));
+  let values = $state.raw<Record<string, DialValue>>(DialStore.getValues(untrack(() => meta.id)));
+  let transport = $state.raw<TimelineTransport>(TimelineStore.getTransport(untrack(() => meta.id)));
   let laneArea = $state<HTMLDivElement>();
   let horizontalScrollElement = $state<HTMLDivElement>();
   let laneWidth = $state(0);
diff --git a/dist/svelte/createDialKit.svelte.js b/dist/svelte/createDialKit.svelte.js
index d6d44b18e6512d76869208a2d5a5649f98776531..0573abf139bb7a0f8cda9662c789dfa112c91282 100644
--- a/dist/svelte/createDialKit.svelte.js
+++ b/dist/svelte/createDialKit.svelte.js
@@ -7,7 +7,7 @@ export function createDialKitController(name, config, options) {
     const hasStableId = options?.id !== undefined;
     const panelId = options?.id ?? `${name}-${++dialKitInstance}`;
     const resolve = () => resolveDialValues(config, DialStore.getValues(panelId));
-    let values = $state(resolve());
+    let values = $state.raw(resolve());
     $effect(() => {
         DialStore.registerPanel(panelId, name, config, options?.shortcuts, {
             retainOnUnmount: hasStableId,
diff --git a/dist/svelte/createDialTimeline.svelte.js b/dist/svelte/createDialTimeline.svelte.js
index feee1b655bcbed8a5dabffb76eb080f3bc218e2a..75fb123158c114772c650b0db8be62c9de80421d 100644
--- a/dist/svelte/createDialTimeline.svelte.js
+++ b/dist/svelte/createDialTimeline.svelte.js
@@ -1,3 +1,4 @@
+import { untrack } from 'svelte';
 import { DialStore } from 'dialkit/store';
 import { TimelineStore, buildTimelineMeta, buildTimelineValues, computeStaticTimeline, parseTimelineConfig, resolveTimelineLoop, } from 'dialkit/timeline';
 let timelineInstance = 0;
@@ -5,8 +6,8 @@ export function createDialTimeline(name, config, options) {
     const hasStableId = options?.id !== undefined;
     const panelId = options?.id ?? `${name}-${++timelineInstance}`;
     const parsed = $derived(parseTimelineConfig(config));
-    let flatValues = $state(DialStore.getValues(panelId));
-    let transport = $state(TimelineStore.getTransport(panelId));
+    let flatValues = $state.raw(DialStore.getValues(panelId));
+    let transport = $state.raw(TimelineStore.getTransport(panelId));
     const staticTimeline = $derived(computeStaticTimeline(parsed, flatValues));
     const actions = {
         play: () => TimelineStore.play(panelId),
@@ -23,7 +24,7 @@ export function createDialTimeline(name, config, options) {
             kind: 'timeline',
         });
         flatValues = DialStore.getValues(panelId);
-        const initialStatic = computeStaticTimeline(currentParsed, flatValues);
+        const initialStatic = computeStaticTimeline(currentParsed, untrack(() => flatValues));
         TimelineStore.register(buildTimelineMeta(panelId, name, initialStatic.duration, currentParsed, options?.loop), { autoplay: options?.autoplay ?? true });
         transport = TimelineStore.getTransport(panelId);
         const unsubscribeValues = DialStore.subscribe(panelId, () => {
@@ -40,7 +41,14 @@ export function createDialTimeline(name, config, options) {
         };
     });
     $effect(() => {
-        TimelineStore.update(buildTimelineMeta(panelId, name, staticTimeline.duration, parsed, options?.loop));
+        const meta = buildTimelineMeta(panelId, name, staticTimeline.duration, parsed, options?.loop);
+        // Only broadcast a change. `update` notifies every dock subscriber even
+        // for identical meta, and those subscribers re-enter this effect.
+        const prev = untrack(() => TimelineStore.getTimeline(panelId));
+        if (prev && prev.duration === meta.duration && prev.clips === meta.clips && prev.loop === meta.loop && prev.loopStart === meta.loopStart) {
+            return;
+        }
+        TimelineStore.update(meta);
     });
     return reactiveProxy(() => resolved);
 }
